
kind: Deployment
apiVersion: apps/v1
metadata:
  name: go-random-quotes
spec:
  replicas: 1
  selector:
    matchLabels:
      app: go-random-quotes
  template:
    metadata:
      labels:
        app: go-random-quotes
    spec:
      containers:
      - name: go-random-quotes
        image: gcr.io/PROJECT_ID/IMAGE:TAG
        resources:
          limits:
            memory: "500Mi"
            cpu: "100m"
        imagePullPolicy: Always
        ports:
        - name: go-random-quotes
          containerPort: 80